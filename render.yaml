# =====================================================================
# RENDER BLUEPRINT - FULLSTACK DEPLOYMENT
# =====================================================================
# This file automates deployment to Render.com.
# 1. Create a dynamic Blueprint on Render.
# 2. Connect your GitHub repository.
# 3. Render will provision all resources.
# =====================================================================

databases:
  - name: ecommerce-db
    databaseName: ecommerce
    user: postgres
    plan: free

services:
  # 1. Redis Service (Internal)
  - type: redis
    name: ecommerce-redis
    ipAllowList: []
    plan: free

  # 2. API Service (NestJS)
  - type: web
    name: ecommerce-api
    env: node
    plan: free
    rootDir: api
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npx prisma db push && npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: ecommerce-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: ecommerce-redis
          property: connectionString
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://ecommerce-web.onrender.com # Placeholder - Update after web deploy

  # 3. Web Service (Next.js)
  - type: web
    name: ecommerce-web
    env: node
    plan: free
    rootDir: web
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: API_URL
        value: http://ecommerce-api:10000/api/v1
      - key: NEXT_PUBLIC_API_URL
        sync: false # Set this in Render dashboard after API service is live
        placeholder: "https://ecommerce-api.onrender.com/api/v1"
