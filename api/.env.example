# =====================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =====================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git!
#
# To generate secure secrets:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# =====================================================================

# Environment
NODE_ENV=development
PORT=8080

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/ecommerce?schema=public
DATABASE_POOL_SIZE=10

# Redis
REDIS_URL=redis://localhost:6380

# JWT Authentication (REQUIRED - Generate with crypto.randomBytes(64))
# CRITICAL: Must be at least 32 characters!
JWT_ACCESS_SECRET=CHANGE_ME_TO_RANDOM_64_CHAR_STRING_USE_CRYPTO_RANDOMBYTES
JWT_REFRESH_SECRET=CHANGE_ME_TO_RANDOM_64_CHAR_STRING_USE_CRYPTO_RANDOMBYTES
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Frontend URLs
FRONTEND_URL=http://localhost:3000

# Cloudinary (Image Upload) - REQUIRED IN PRODUCTION
# Sign up at https://cloudinary.com
CLOUDITARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# Optional: Email Service (Nodemailer)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Google Gemini AI
# GOOGLE_GEMINI_API_KEY=your_gemini_api_key

# Optional: Sentry (Error Tracking)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Optional: OAuth Providers
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# FACEBOOK_APP_ID=
# FACEBOOK_APP_SECRET=
